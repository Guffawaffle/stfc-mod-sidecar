{
  "name": "@stfc-mod-sidecar/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Electron shell for the STFC Sidecar UI: tray, always-on-top, single-instance, child-process server lifecycle.",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "electron .",
    "dev": "ELECTRON_DEV=1 electron .",
    "package": "electron-forge package",
    "make": "electron-forge make"
  },
  "dependencies": {
    "@stfc-mod-sidecar/server": "*"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.5.0",
    "@electron-forge/maker-squirrel": "^7.5.0",
    "@electron-forge/maker-zip": "^7.5.0",
    "@types/node": "^22.15.30",
    "electron": "^33.0.0",
    "typescript": "^5.9.3"
  }
}
